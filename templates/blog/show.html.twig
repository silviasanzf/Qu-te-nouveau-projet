{# templates/blog/show.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}{{ slug }}{% endblock %}

{% block stylesheets %}{% endblock %}

{% block body %}

    <div>
        <h1>Real articles title : {{ article.title }}</h1>
        <h2>Category : {{ category.name }}</h2>
        <p>{{ article.content }}</p>
    </div>

    <a href="{{ path('homepage') }}">
        Back to homepage.
    </a>

{% endblock %}